package cn.qianlicao.gank.data.gank

import java.util.concurrent.ConcurrentHashMap

/**
 * Created by dongyayun on 16/3/13.
 */

class AllItems {

    var itemsMap: ConcurrentHashMap<Category, List<GankItem>> = ConcurrentHashMap();

    //    var itemsOfiOS: List<GankItem> ? = null
    //    var itemsOfRecommend: List<GankItem> ? = null
    //    var itemsOfExtendResource: List<GankItem> ? = null
    //    var itemsOfRestVideo: List<GankItem> ? = null
    //    var itemsOfFronted: List<GankItem> ? = null
    //    var itemsOfAndroid: List<GankItem> ? = null
    //    var itemsOfSex: List<GankItem> ? = null

}
